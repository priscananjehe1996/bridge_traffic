<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Uganda Bridge Traffic Analytics - Network Influence Dashboard</title>
<meta name="description" content="Classified traffic counts, ADT context, and network loading implications for 546 bridge crossings in Uganda.">
<link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'%3E%3Crect width='32' height='32' rx='6' fill='%23060913'/%3E%3Cpath d='M5 18h22' stroke='%2300e5ff' stroke-width='3' stroke-linecap='round'/%3E%3Cpath d='M8 13h14l4 5H4z' fill='%235474ff' opacity='.8'/%3E%3C/svg%3E">
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --bg-primary: #060913;
  --bg-card: rgba(13, 20, 38, 0.82);
  --bg-card-hover: rgba(20, 30, 58, 0.95);
  --border: rgba(99, 130, 255, 0.12);
  --border-glow: rgba(99, 130, 255, 0.35);
  --text-primary: #f0f3fa;
  --text-secondary: #9aa5c4;
  --text-muted: #5a668a;
  --accent-blue: #5474ff;
  --accent-cyan: #00e5ff;
  --accent-purple: #b55fe6;
  --accent-emerald: #10b981;
  --accent-amber: #f59e0b;
  --accent-rose: #f43f5e;
  --accent-indigo: #818cf8;
  --gradient-primary: linear-gradient(135deg, #5474ff 0%, #b55fe6 100%);
  --gradient-card: linear-gradient(135deg, rgba(13,20,38,0.9) 0%, rgba(18,27,51,0.85) 100%);
  --shadow-glow: 0 0 40px rgba(84, 116, 255, 0.09);
  --radius: 10px;
  --radius-sm: 8px;
  --transition: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

body {
  font-family: 'Plus Jakarta Sans', -apple-system, BlinkMacSystemFont, sans-serif;
  background: var(--bg-primary);
  color: var(--text-primary);
  min-height: 100vh;
  overflow-x: hidden;
  line-height: 1.5;
}

body::before {
  content: '';
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background:
    radial-gradient(ellipse 60% 40% at 50% -10%, rgba(84,116,255,0.15) 0%, transparent 60%),
    radial-gradient(ellipse 50% 30% at 90% 90%, rgba(181,95,230,0.08) 0%, transparent 50%),
    radial-gradient(ellipse 40% 30% at 10% 50%, rgba(0,229,255,0.05) 0%, transparent 50%);
  pointer-events: none;
  z-index: 0;
}

.container { width: 100%; max-width: none; margin: 0 auto; padding: 0 14px; position: relative; z-index: 1; }

/* HEADER */
header {
  padding: 22px 0 16px;
  text-align: center;
  position: relative;
}

header::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 180px;
  height: 2px;
  background: var(--gradient-primary);
  border-radius: 3px;
}

.header-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 5px 12px;
  background: rgba(84,116,255,0.08);
  border: 1px solid rgba(84,116,255,0.22);
  border-radius: 100px;
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1.8px;
  color: var(--accent-blue);
  margin-bottom: 10px;
}

.header-badge svg { width: 14px; height: 14px; }

h1 {
  font-size: clamp(1.7rem, 3vw, 2.55rem);
  font-weight: 800;
  background: linear-gradient(135deg, #f0f3fa 0%, #5474ff 60%, #b55fe6 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  line-height: 1.15;
  margin-bottom: 6px;
  letter-spacing: -0.5px;
}

header p {
  font-size: 12.5px;
  color: var(--text-secondary);
  max-width: 1120px;
  margin: 0 auto;
  line-height: 1.65;
}

/* KPI ROW */
.kpi-row {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
  gap: 10px;
  margin: 16px 0;
}

.kpi-card {
  background: var(--gradient-card);
  backdrop-filter: blur(24px);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 16px 14px;
  text-align: center;
  transition: var(--transition);
  position: relative;
  overflow: hidden;
}

.kpi-card::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 3px;
  border-radius: var(--radius) var(--radius) 0 0;
}

.kpi-card:nth-child(1)::before { background: var(--accent-blue); }
.kpi-card:nth-child(2)::before { background: var(--accent-cyan); }
.kpi-card:nth-child(3)::before { background: var(--accent-purple); }
.kpi-card:nth-child(4)::before { background: var(--accent-emerald); }
.kpi-card:nth-child(5)::before { background: var(--accent-amber); }
.kpi-card:nth-child(6)::before { background: var(--accent-rose); }

.kpi-card:hover {
  border-color: var(--border-glow);
  box-shadow: var(--shadow-glow);
  transform: translateY(-3px);
}

.kpi-value {
  font-size: 1.65rem;
  font-weight: 800;
  color: var(--text-primary);
  line-height: 1.1;
  margin-bottom: 6px;
  letter-spacing: -0.5px;
}

.kpi-label {
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1.2px;
  color: var(--text-secondary);
}

/* TOOLBAR */
.toolbar {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  align-items: center;
  margin: 14px 0 12px;
  padding: 10px 12px;
  background: var(--gradient-card);
  backdrop-filter: blur(24px);
  border: 1px solid var(--border);
  border-radius: var(--radius);
}

.toolbar label {
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1.2px;
  color: var(--text-secondary);
}

.toolbar select, .toolbar input[type="text"] {
  background: rgba(255,255,255,0.03);
  border: 1px solid var(--border);
  color: var(--text-primary);
  padding: 8px 10px;
  border-radius: var(--radius-sm);
  font-family: inherit;
  font-size: 13px;
  font-weight: 500;
  outline: none;
  transition: var(--transition);
  min-width: 180px;
}

.toolbar select:focus, .toolbar input:focus {
  border-color: var(--accent-blue);
  box-shadow: 0 0 0 3px rgba(84,116,255,0.2);
  background: rgba(255,255,255,0.05);
}

.toolbar select option { background: #111111; color: #ffffff; }

.btn-group { display: flex; gap: 8px; margin-left: auto; }

.btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  background: rgba(255,255,255,0.03);
  color: var(--text-primary);
  font-family: inherit;
  font-size: 12px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  transition: var(--transition);
}

.btn:hover { background: rgba(84,116,255,0.12); border-color: var(--accent-blue); }
.btn svg { width: 15px; height: 15px; }

.btn-primary {
  background: var(--gradient-primary);
  border: none;
  color: #fff;
}
.btn-primary:hover {
  opacity: 0.92;
  transform: translateY(-1px);
  box-shadow: 0 4px 18px rgba(84,116,255,0.35);
}

/* TABS */
.tabs {
  display: flex;
  gap: 4px;
  margin: 12px 0 12px;
  background: rgba(13,20,38,0.7);
  padding: 4px;
  border-radius: var(--radius-sm);
  border: 1px solid var(--border);
  overflow-x: auto;
}

.tab {
  padding: 8px 12px;
  border: none;
  background: none;
  color: var(--text-secondary);
  font-family: inherit;
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  border-radius: 8px;
  transition: var(--transition);
  white-space: nowrap;
}

.tab:hover { color: var(--text-primary); background: rgba(255,255,255,0.03); }
.tab.active { background: var(--gradient-primary); color: #fff; }

/* PANELS */
.panel { display: none; animation: fadeIn 0.45s cubic-bezier(0.4, 0, 0.2, 1); }
.panel.active { display: block; }

@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: none; } }

/* CARDS */
.card {
  background: var(--gradient-card);
  backdrop-filter: blur(24px);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 16px;
  margin-bottom: 12px;
  transition: var(--transition);
}

.card:hover { border-color: rgba(84,116,255,0.18); }

.card-title {
  font-size: 14px;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 6px;
}

.card-subtitle {
  font-size: 11px;
  color: var(--text-muted);
  margin-bottom: 12px;
}

/* CHARTS */
.chart-container {
  position: relative;
  width: 100%;
  height: 320px;
}

.chart-container canvas { width: 100% !important; height: 100% !important; }

.chart-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}

@media (max-width: 960px) { .chart-row { grid-template-columns: 1fr; } }

/* TABLE */
.table-wrap {
  width: 100%;
  max-width: 100%;
  overflow-x: auto;
  overflow-y: hidden;
  white-space: nowrap;
  border-radius: var(--radius-sm);
  border: 1px solid var(--border);
  background: rgba(13,20,38,0.4);
  scrollbar-gutter: stable;
}

.table-wrap::-webkit-scrollbar {
  height: 12px;
}

.table-wrap::-webkit-scrollbar-thumb {
  background: rgba(0,229,255,0.32);
  border: 3px solid rgba(13,20,38,0.92);
  border-radius: 999px;
}

.table-wrap::-webkit-scrollbar-track {
  background: rgba(6,9,19,0.42);
  border-radius: 999px;
}

.bridge-inventory-scroll {
  display: block;
  width: 100%;
  max-width: calc(100vw - 34px) !important;
  height: min(62vh, 620px);
  overflow-x: auto !important;
  overflow-y: auto !important;
  padding-bottom: 0;
  scrollbar-gutter: stable both-edges;
  overscroll-behavior-x: contain;
  touch-action: pan-x pan-y;
  scrollbar-width: auto;
  background: rgba(8, 13, 29, 0.92);
  border: 1px solid var(--border);
  border-radius: 7px;
}

#bridgeTable {
  width: max-content;
  min-width: 5600px;
  table-layout: auto;
  border-collapse: separate;
  border-spacing: 0;
  background: rgba(8, 13, 29, 0.92);
  color: var(--text-primary);
}

.inventory-scroll-proxy {
  display: none !important;
  height: 0;
  margin: 0;
  pointer-events: none;
  overflow-x: auto;
  overflow-y: hidden;
  border-radius: 999px;
  background: rgba(5, 9, 23, 0.96);
  border: 1px solid rgba(84, 116, 255, 0.24);
  box-shadow: 0 10px 32px rgba(0, 0, 0, 0.46);
}

.inventory-scroll-proxy > div {
  height: 1px;
}

.inventory-scroll-proxy::-webkit-scrollbar {
  height: 14px;
}

.inventory-scroll-proxy::-webkit-scrollbar-thumb {
  background: rgba(190, 198, 211, 0.72);
  border: 4px solid rgba(8, 13, 29, 0.96);
  border-radius: 999px;
}

.inventory-scroll-proxy::-webkit-scrollbar-track {
  background: rgba(5, 9, 23, 0.96);
  border-radius: 999px;
}

#bridgeTable th,
#bridgeTable td {
  min-width: 120px;
  color: var(--text-secondary);
  border-bottom: 1px solid rgba(84, 116, 255, 0.10);
  white-space: normal;
  vertical-align: middle;
}

#bridgeTable thead th {
  position: sticky;
  top: 0;
  z-index: 5;
  background: linear-gradient(180deg, rgba(14, 20, 39, 0.98), rgba(9, 14, 30, 0.98));
  color: var(--accent-cyan) !important;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .65px;
  text-transform: uppercase;
  border-bottom: 0;
  border-right: 1px solid rgba(84, 116, 255, 0.13);
  box-shadow: 0 1px 0 rgba(95, 232, 255, 0.16);
}

#bridgeTable tbody td {
  font-size: 11px;
  font-weight: 700;
  line-height: 1.22;
  padding: 14px 8px;
  font-family: 'Plus Jakarta Sans', sans-serif;
}

#bridgeTable tbody tr:nth-child(odd) {
  background: rgba(13, 20, 38, 0.92);
}

#bridgeTable tbody tr:nth-child(even) {
  background: rgba(8, 13, 29, 0.86);
}

#bridgeTable tbody tr:hover {
  background: rgba(84, 116, 255, 0.14);
}

#bridgeTable .highlight-cell {
  color: var(--text-primary) !important;
  font-weight: 900;
}

#bridgeTable .pill,
#bridgeTable .rating-badge {
  box-shadow: var(--shadow-glow);
  border-color: rgba(95, 232, 255, 0.16);
}

.analytics-grid-scroll {
  display: block;
  width: 100%;
  max-width: calc(100vw - 34px) !important;
  height: min(62vh, 620px);
  overflow-x: auto !important;
  overflow-y: auto !important;
  padding-bottom: 0;
  scrollbar-gutter: stable both-edges;
  overscroll-behavior-x: contain;
  touch-action: pan-x pan-y;
  background: rgba(8, 13, 29, 0.92);
  border: 1px solid var(--border);
  border-radius: 7px;
}

#bridgeTrafficTable {
  width: max-content;
  min-width: 6200px;
  table-layout: auto;
  border-collapse: separate;
  border-spacing: 0;
  background: rgba(8, 13, 29, 0.92);
  color: var(--text-primary);
}

#bridgeTrafficTable th,
#bridgeTrafficTable td {
  min-width: 118px;
  color: var(--text-secondary);
  border-bottom: 1px solid rgba(84, 116, 255, 0.10);
  white-space: normal;
  vertical-align: middle;
}

#bridgeTrafficTable thead th {
  position: sticky;
  top: 0;
  z-index: 5;
  background: linear-gradient(180deg, rgba(14, 20, 39, 0.98), rgba(9, 14, 30, 0.98));
  color: var(--accent-cyan) !important;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .65px;
  text-transform: uppercase;
  border-bottom: 0;
  border-right: 1px solid rgba(84, 116, 255, 0.13);
  box-shadow: 0 1px 0 rgba(95, 232, 255, 0.16);
}

#bridgeTrafficTable tbody td {
  font-size: 11px;
  font-weight: 700;
  line-height: 1.22;
  padding: 14px 8px;
  font-family: 'Plus Jakarta Sans', sans-serif;
}

#bridgeTrafficTable tbody tr:nth-child(odd) {
  background: rgba(13, 20, 38, 0.92);
}

#bridgeTrafficTable tbody tr:nth-child(even) {
  background: rgba(8, 13, 29, 0.86);
}

#bridgeTrafficTable tbody tr:hover {
  background: rgba(84, 116, 255, 0.14);
}

#bridgeTrafficTable .highlight-cell {
  color: var(--text-primary) !important;
  font-weight: 900;
}

#panel-bridge-traffic .network-banner {
  display: none !important;
}

#panel-bridge-traffic > .card {
  padding: 14px;
  background: rgba(10, 16, 34, 0.96);
  border: 1px solid rgba(84, 116, 255, 0.22);
  border-radius: 8px;
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.025);
}

#panel-bridge-traffic .card-title {
  color: var(--text-primary);
  font-size: 13px;
  font-weight: 900;
  letter-spacing: 0;
  text-transform: none;
  margin-bottom: 6px;
}

#panel-bridge-traffic .card-subtitle {
  color: var(--text-muted);
  font-size: 10px;
  font-weight: 650;
  line-height: 1.35;
  margin-bottom: 14px;
}

#panel-bridge-traffic .table-wrap {
  display: block;
  width: 100%;
  max-width: calc(100vw - 34px) !important;
  height: min(62vh, 620px);
  overflow: auto !important;
  scrollbar-gutter: stable both-edges;
  overscroll-behavior-x: contain;
  touch-action: pan-x pan-y;
  background: rgba(8, 13, 29, 0.92);
  border: 1px solid rgba(84, 116, 255, 0.24);
  border-radius: 7px;
}

#panel-bridge-traffic .table-wrap::-webkit-scrollbar,
.analytics-grid-scroll::-webkit-scrollbar {
  width: 13px;
  height: 13px;
}

#panel-bridge-traffic .table-wrap::-webkit-scrollbar-track,
.analytics-grid-scroll::-webkit-scrollbar-track {
  background: rgba(4, 8, 20, 0.98);
  border-radius: 999px;
}

#panel-bridge-traffic .table-wrap::-webkit-scrollbar-thumb,
.analytics-grid-scroll::-webkit-scrollbar-thumb {
  background: linear-gradient(90deg, rgba(36, 121, 148, 0.9), rgba(95, 232, 255, 0.48));
  border: 3px solid rgba(4, 8, 20, 0.98);
  border-radius: 999px;
}

#panel-bridge-traffic .table-wrap::-webkit-scrollbar-corner,
.analytics-grid-scroll::-webkit-scrollbar-corner {
  background: rgba(4, 8, 20, 0.98);
}

#bridgeVehicleClassTable,
#bridgeLoadingTable,
#bridgeOdFlowTable,
#bridgeSeasonalFactorsTable {
  width: max-content;
  table-layout: auto;
  border-collapse: separate;
  border-spacing: 0;
  background: rgba(8, 13, 29, 0.92);
  color: var(--text-primary);
}

#bridgeVehicleClassTable { min-width: 3000px; }
#bridgeLoadingTable { min-width: 1850px; }
#bridgeOdFlowTable { min-width: 2650px; }
#bridgeSeasonalFactorsTable { min-width: 2650px; }

#bridgeVehicleClassTable th,
#bridgeVehicleClassTable td,
#bridgeLoadingTable th,
#bridgeLoadingTable td,
#bridgeOdFlowTable th,
#bridgeOdFlowTable td,
#bridgeSeasonalFactorsTable th,
#bridgeSeasonalFactorsTable td {
  min-width: 118px;
  color: var(--text-secondary);
  border-bottom: 1px solid rgba(84, 116, 255, 0.10);
  white-space: normal;
  vertical-align: middle;
}

#bridgeVehicleClassTable thead th,
#bridgeLoadingTable thead th,
#bridgeOdFlowTable thead th,
#bridgeSeasonalFactorsTable thead th {
  position: sticky;
  top: 0;
  z-index: 5;
  background: linear-gradient(180deg, rgba(14, 20, 39, 0.98), rgba(9, 14, 30, 0.98));
  color: var(--accent-cyan) !important;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .65px;
  text-transform: uppercase;
  border-bottom: 0;
  border-right: 1px solid rgba(84, 116, 255, 0.13);
  box-shadow: 0 1px 0 rgba(95, 232, 255, 0.16);
}

#bridgeVehicleClassTable tbody td,
#bridgeLoadingTable tbody td,
#bridgeOdFlowTable tbody td,
#bridgeSeasonalFactorsTable tbody td {
  font-size: 11px;
  font-weight: 700;
  line-height: 1.22;
  padding: 14px 8px;
  font-family: 'Plus Jakarta Sans', sans-serif;
}

#bridgeVehicleClassTable tbody tr:nth-child(odd),
#bridgeLoadingTable tbody tr:nth-child(odd),
#bridgeOdFlowTable tbody tr:nth-child(odd),
#bridgeSeasonalFactorsTable tbody tr:nth-child(odd) {
  background: rgba(13, 20, 38, 0.92);
}

#bridgeVehicleClassTable tbody tr:nth-child(even),
#bridgeLoadingTable tbody tr:nth-child(even),
#bridgeOdFlowTable tbody tr:nth-child(even),
#bridgeSeasonalFactorsTable tbody tr:nth-child(even) {
  background: rgba(8, 13, 29, 0.86);
}

#bridgeVehicleClassTable tbody tr:hover,
#bridgeLoadingTable tbody tr:hover,
#bridgeOdFlowTable tbody tr:hover,
#bridgeSeasonalFactorsTable tbody tr:hover {
  background: rgba(84, 116, 255, 0.14);
}

#bridgeVehicleClassTable .highlight-cell,
#bridgeLoadingTable .highlight-cell,
#bridgeOdFlowTable .highlight-cell,
#bridgeSeasonalFactorsTable .highlight-cell {
  color: var(--text-primary) !important;
  font-weight: 900;
}

.inventory-pager {
  display: flex;
  justify-content: flex-end;
  align-items: center;
  gap: 10px;
  padding: 14px 0 2px;
}

.inventory-pager button {
  min-height: 38px;
  padding: 8px 12px;
  border: 1px solid var(--border);
  border-radius: 8px;
  background: rgba(14, 20, 39, 0.88);
  color: var(--text-primary);
  font-size: 11px;
  font-weight: 800;
  cursor: pointer;
}

.inventory-pager button:disabled {
  background: rgba(91, 103, 133, 0.28);
  color: var(--text-muted);
  cursor: not-allowed;
}

.inventory-pager span {
  color: var(--text-secondary);
  font-size: 12px;
  font-weight: 800;
}

#bridgeTable th:nth-child(1),
#bridgeTable td:nth-child(1) {
  min-width: 190px;
}

#bridgeTable th:nth-child(3),
#bridgeTable td:nth-child(3),
#bridgeTable th:nth-child(14),
#bridgeTable td:nth-child(14) {
  min-width: 260px;
}

table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
  font-size: 13px;
  white-space: nowrap;
}

thead th {
  position: sticky;
  top: 0;
  background: #0d1426;
  padding: 9px 10px;
  text-align: left;
  font-weight: 700;
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 1.2px;
  color: var(--accent-blue);
  border-bottom: 2px solid var(--border);
  white-space: nowrap;
  cursor: pointer;
  user-select: none;
  transition: var(--transition);
}

thead th:hover { color: var(--accent-cyan); background: rgba(84,116,255,0.04); }

/* Sorting indicators */
thead th.sorted-asc::after { content: ' ^'; color: var(--accent-cyan); }
thead th.sorted-desc::after { content: ' v'; color: var(--accent-cyan); }

tbody tr { transition: var(--transition); cursor: pointer; }
tbody tr:hover { background: rgba(84,116,255,0.06); }
tbody tr.active-bridge-row {
  background: rgba(38, 230, 246, 0.14) !important;
  outline: 1px solid rgba(38, 230, 246, 0.55);
  outline-offset: -1px;
}

tbody tr.active-bridge-row td:first-child,
tbody tr.active-bridge-row .highlight-cell {
  color: var(--accent-cyan);
}

tbody td {
  padding: 8px 10px;
  border-bottom: 1px solid rgba(84,116,255,0.05);
  color: var(--text-secondary);
  white-space: nowrap;
}

tbody tr:last-child td { border-bottom: none; }

tbody td.highlight-cell {
  color: var(--text-primary);
  font-weight: 600;
}

/* INFRASTRUCTURE PROGRESS BAR */
.progress-bar-container {
  width: 120px;
  height: 8px;
  background: rgba(255,255,255,0.06);
  border-radius: 10px;
  overflow: hidden;
  display: inline-block;
  vertical-align: middle;
  margin-right: 8px;
}

.progress-bar-fill {
  height: 100%;
  border-radius: 10px;
  background: var(--gradient-primary);
}

.progress-pct {
  font-size: 11px;
  font-weight: 700;
  color: var(--text-primary);
  vertical-align: middle;
  margin-left: 4px;
}

/* Badges */
.badge {
  display: inline-flex;
  align-items: center;
  padding: 3px 8px;
  border-radius: 6px;
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.badge-behind { background: rgba(244,63,94,0.12); color: var(--accent-rose); border: 1px solid rgba(244,63,94,0.2); }
.badge-ontime { background: rgba(16,185,129,0.12); color: var(--accent-emerald); border: 1px solid rgba(16,185,129,0.2); }
.badge-completed { background: rgba(0,229,255,0.12); color: var(--accent-cyan); border: 1px solid rgba(0,229,255,0.2); }

/* Rating badge */
.rating-badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 26px;
  height: 26px;
  border-radius: 6px;
  font-size: 12px;
  font-weight: 800;
}

.rating-7 { background: rgba(16,185,129,0.15); color: #10b981; }
.rating-6 { background: rgba(16,185,129,0.15); color: #10b981; }
.rating-5 { background: rgba(245,158,11,0.15); color: #f59e0b; }
.rating-4 { background: rgba(245,158,11,0.15); color: #f59e0b; }
.rating-low { background: rgba(244,63,94,0.2); color: #f43f5e; }

/* MODALS (Bridge Details) */
.modal-overlay {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(4, 7, 16, 0.75);
  backdrop-filter: blur(8px);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  pointer-events: none;
  transition: 0.35s cubic-bezier(0.4, 0, 0.2, 1);
}

.modal-overlay.active {
  opacity: 1;
  pointer-events: auto;
}

.modal-content {
  background: #0a0e1c;
  border: 1px solid rgba(84, 116, 255, 0.25);
  box-shadow: 0 20px 60px rgba(0,0,0,0.6), 0 0 50px rgba(84,116,255,0.08);
  border-radius: 24px;
  width: 90%;
  max-width: 980px;
  max-height: 85vh;
  overflow-y: auto;
  transform: scale(0.95);
  transition: 0.35s cubic-bezier(0.4, 0, 0.2, 1);
}

.modal-overlay.active .modal-content {
  transform: scale(1);
}

.modal-header {
  padding: 16px 20px;
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: rgba(13, 20, 38, 0.6);
}

.modal-header h3 {
  font-size: 20px;
  font-weight: 800;
  background: linear-gradient(135deg, #fff 0%, #5474ff 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  letter-spacing: -0.3px;
}

.modal-close {
  background: rgba(255,255,255,0.03);
  border: 1px solid var(--border);
  border-radius: 50%;
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-secondary);
  font-size: 22px;
  cursor: pointer;
  transition: var(--transition);
}

.modal-close:hover {
  background: rgba(244,63,94,0.1);
  border-color: var(--accent-rose);
  color: var(--accent-rose);
}

.modal-body {
  padding: 18px;
}

.modal-grid-section {
  margin-bottom: 16px;
}

.modal-section-title {
  font-size: 12px;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  color: var(--accent-cyan);
  margin-bottom: 16px;
  border-left: 3px solid var(--accent-cyan);
  padding-left: 10px;
}

.modal-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
  gap: 10px;
}

.modal-card {
  background: rgba(13,20,38,0.5);
  border: 1px solid rgba(84,116,255,0.08);
  border-radius: 12px;
  padding: 10px 12px;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.modal-card-label {
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: var(--text-muted);
}

.modal-card-value {
  font-size: 14px;
  font-weight: 600;
  color: var(--text-primary);
}

.modal-card-value.highlight {
  color: var(--accent-cyan);
  font-weight: 700;
}

/* Empty states */
.empty-state {
  text-align: center;
  padding: 28px 16px;
  color: var(--text-muted);
}

.empty-state svg { width: 56px; height: 56px; margin-bottom: 16px; opacity: 0.35; color: var(--accent-blue); }
.empty-state p { font-size: 14px; max-width: 420px; margin: 0 auto; line-height: 1.6; }

/* WIDER NETWORK CONTEXT BANNER */
.network-banner {
  background: linear-gradient(135deg, rgba(84,116,255,0.08) 0%, rgba(181,95,230,0.03) 100%);
  border: 1px solid rgba(84,116,255,0.2);
  border-radius: var(--radius);
  padding: 12px 14px;
  margin-bottom: 12px;
  display: flex;
  align-items: flex-start;
  gap: 16px;
}

.network-banner-icon {
  background: rgba(84,116,255,0.12);
  border: 1px solid rgba(84,116,255,0.22);
  border-radius: 12px;
  padding: 10px;
  color: var(--accent-cyan);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.network-banner-icon svg { width: 22px; height: 22px; }

.network-banner-content h4 {
  font-size: 14px;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 4px;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.network-banner-content p {
  font-size: 12.5px;
  color: var(--text-secondary);
  line-height: 1.65;
}

.pill {
  display: inline-block;
  padding: 3px 10px;
  border-radius: 100px;
  font-size: 10.5px;
  font-weight: 700;
  letter-spacing: 0.5px;
  text-transform: uppercase;
}

.pill-a { background: rgba(84,116,255,0.12); color: #7390ff; border: 1px solid rgba(84,116,255,0.2); }
.pill-b { background: rgba(181,95,230,0.12); color: #cb84fc; border: 1px solid rgba(181,95,230,0.2); }
.pill-c { background: rgba(0,229,255,0.12); color: #4bf0ff; border: 1px solid rgba(0,229,255,0.2); }
.pill-m { background: rgba(168,85,247,0.14); color: #c084fc; border: 1px solid rgba(168,85,247,0.28); }

footer {
  text-align: center;
  padding: 20px 0;
  font-size: 12px;
  color: var(--text-muted);
  border-top: 1px solid var(--border);
  margin-top: 18px;
  background: rgba(4,7,16,0.3);
}

footer a { color: var(--accent-blue); text-decoration: none; font-weight: 600; }
footer a:hover { text-decoration: underline; color: var(--accent-cyan); }

::-webkit-scrollbar { width: 8px; height: 8px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(84,116,255,0.18); border-radius: 4px; }
::-webkit-scrollbar-thumb:hover { background: rgba(84,116,255,0.32); }

#exportCanvas { display: none; }

.traffic-context-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 10px;
  margin-bottom: 12px;
}

.traffic-metric {
  background: rgba(255,255,255,0.03);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 10px 12px;
}

.traffic-metric strong {
  display: block;
  color: var(--text-primary);
  font-size: 20px;
  line-height: 1.15;
  margin-bottom: 4px;
}

.traffic-metric span {
  color: var(--text-muted);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1px;
  text-transform: uppercase;
}

.map-stat-row {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 8px;
  margin: 0 0 10px;
}

.map-stat {
  background: rgba(6,9,19,0.45);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 8px 10px;
}

.map-stat strong { display:block; font-size: 16px; line-height: 1.1; color: var(--text-primary); }
.map-stat span { display:block; margin-top: 3px; font-size: 9px; font-weight: 800; color: var(--text-muted); text-transform: uppercase; letter-spacing: .8px; }

.map-workspace {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 360px;
  gap: 12px;
  align-items: stretch;
}

.map-stage {
  position: relative;
  width: 100%;
  height: min(76vh, 760px);
  min-height: 560px;
  background: #040710;
  border-radius: 8px;
  border: 1px solid var(--border);
  overflow: hidden;
}

.calendar-panel {
  position: absolute;
  right: 14px;
  bottom: 134px;
  width: min(360px, calc(100% - 28px));
  min-height: 292px;
  display: none;
  background: rgba(5, 10, 22, 0.68);
  border: 1px solid rgba(0,229,255,0.24);
  border-radius: 12px;
  z-index: 1000;
  backdrop-filter: blur(10px);
  box-shadow: 0 14px 34px rgba(0,0,0,0.34);
  overflow: hidden;
}

.calendar-layout {
  display: grid;
  grid-template-columns: 98px 1fr;
  min-height: 292px;
}

.calendar-months {
  padding: 40px 0 10px;
  border-right: 1px solid rgba(255,255,255,0.08);
  background: rgba(9, 14, 28, 0.34);
}

.calendar-month-btn {
  width: 100%;
  border: 0;
  border-radius: 0;
  padding: 5px 12px;
  background: transparent;
  color: rgba(226,232,240,0.66);
  font-size: 9px;
  font-weight: 900;
  letter-spacing: 1.4px;
  text-align: left;
  cursor: pointer;
}

.calendar-month-btn.active {
  background: rgba(56,189,248,0.14);
  color: var(--accent-cyan);
  border-left: 3px solid var(--accent-cyan);
}

.calendar-main {
  padding: 12px 14px 10px;
}

.calendar-top {
  display: grid;
  grid-template-columns: auto 1fr auto;
  gap: 10px;
  align-items: start;
  margin-bottom: 10px;
}

.calendar-year-nav {
  display: flex;
  align-items: center;
  gap: 7px;
  color: #65caff;
  font-size: 15px;
  font-weight: 900;
  letter-spacing: 2px;
}

.calendar-year-nav button,
.calendar-close {
  border: 0;
  background: transparent;
  color: #65caff;
  font-size: 18px;
  font-weight: 800;
  cursor: pointer;
  line-height: 1;
}

.calendar-close {
  color: rgba(226,232,240,0.62);
  font-size: 18px;
}

.calendar-month-heading {
  color: var(--accent-cyan);
  font-size: 14px;
  font-weight: 900;
  letter-spacing: 4px;
  text-transform: uppercase;
  line-height: 1;
}

.calendar-year-small {
  margin-top: 5px;
  color: rgba(226,232,240,0.78);
  font-size: 8px;
  font-weight: 900;
  letter-spacing: 3px;
}

.calendar-weekdays,
.calendar-days {
  display: grid;
  grid-template-columns: repeat(7, minmax(0, 1fr));
  gap: 5px;
  align-items: center;
}

.calendar-weekdays {
  margin-bottom: 6px;
  color: rgba(226,232,240,0.62);
  font-size: 9px;
  font-weight: 900;
  text-align: center;
}

.calendar-day {
  aspect-ratio: 1 / 1;
  border: 1px solid transparent;
  border-radius: 999px;
  background: rgba(56,189,248,0.10);
  color: rgba(226,232,240,0.82);
  font-size: 11px;
  font-weight: 800;
  cursor: pointer;
}

.calendar-day.adjacent {
  opacity: 0.28;
}

.calendar-day.active {
  border-color: rgba(255,255,255,0.86);
  box-shadow: 0 0 0 3px rgba(255,255,255,0.1);
  color: #ffffff;
}

.calendar-day.today {
  background: rgba(56,189,248,0.26);
}

.calendar-today {
  margin: 5px 0 6px;
  text-align: center;
  color: rgba(226,232,240,0.74);
  font-size: 9px;
  font-weight: 900;
}

.calendar-dots {
  display: flex;
  justify-content: center;
  gap: 4px;
  padding-top: 6px;
  border-top: 1px solid rgba(255,255,255,0.08);
}

.calendar-dot {
  width: 6px;
  height: 6px;
  border-radius: 999px;
  background: rgba(226,232,240,0.28);
}

.calendar-dot.active {
  background: var(--accent-cyan);
}

.timeline-panel {
  position: absolute;
  left: 50%;
  bottom: 18px;
  transform: translateX(-50%);
  width: min(520px, calc(100% - 32px));
  background: rgba(8, 13, 26, 0.9);
  border: 1px solid rgba(0,229,255,0.3);
  border-radius: 16px;
  padding: 18px 22px 16px;
  z-index: 999;
  backdrop-filter: blur(16px);
  box-shadow: 0 14px 40px rgba(0,0,0,0.35);
}

.timeline-head {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 10px;
  align-items: center;
  margin-bottom: 14px;
}

.timeline-title {
  display: flex;
  align-items: center;
  gap: 8px;
  color: var(--text-secondary);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 1.1px;
  text-transform: uppercase;
}

.timeline-date {
  color: var(--text-primary);
  font-size: 20px;
  font-weight: 800;
}

.timeline-controls {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 12px;
  align-items: end;
}

.timeline-controls button {
  width: 36px;
  height: 36px;
  border-radius: 8px;
  border: 1px solid rgba(0,229,255,0.4);
  background: rgba(0,229,255,0.08);
  color: var(--accent-cyan);
  font-size: 16px;
  font-weight: 800;
  cursor: pointer;
}

#timelineSlider {
  width: 100%;
  accent-color: var(--accent-cyan);
}

.timeline-periods {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 6px;
  margin-bottom: 12px;
}

.timeline-periods button {
  height: 28px;
  border-radius: 6px;
  border: 1px solid rgba(84,116,255,0.35);
  background: rgba(15,23,42,0.78);
  color: var(--text-secondary);
  font-size: 11px;
  font-weight: 900;
  cursor: pointer;
}

.timeline-periods button.active {
  border-color: rgba(0,229,255,0.85);
  color: var(--accent-cyan);
  background: rgba(0,229,255,0.08);
}

.timeline-meta {
  display: flex;
  justify-content: space-between;
  margin-top: 6px;
  font-size: 10px;
  font-weight: 800;
  color: var(--text-muted);
}

@media (max-width: 720px) {
  .calendar-panel { right: 10px; bottom: 142px; width: min(330px, calc(100% - 20px)); min-height: 280px; }
  .calendar-layout { grid-template-columns: 88px 1fr; min-height: 280px; }
  .calendar-months { padding-top: 38px; }
  .calendar-month-btn { padding: 5px 10px; font-size: 8.5px; letter-spacing: 1px; }
  .calendar-main { padding: 11px 10px 9px; }
  .calendar-weekdays, .calendar-days { gap: 4px; }
  .timeline-panel { width: calc(100% - 20px); padding: 16px 14px; }
}

.map-side-pane {
  background: rgba(8, 13, 26, 0.92);
  border: 1px solid rgba(84,116,255,0.18);
  border-radius: 10px;
  min-height: 560px;
  height: min(76vh, 760px);
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.pane-header {
  padding: 14px;
  border-bottom: 1px solid var(--border);
  background: rgba(13,20,38,0.72);
}

.pane-eyebrow {
  color: var(--accent-cyan);
  font-size: 10px;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 1.2px;
  margin-bottom: 6px;
}

.pane-title {
  font-size: 18px;
  line-height: 1.2;
  font-weight: 800;
  color: var(--text-primary);
}

.pane-subtitle {
  margin-top: 6px;
  color: var(--text-muted);
  font-size: 11px;
  line-height: 1.45;
}

.pane-body {
  overflow-y: auto;
  padding: 12px 14px 14px;
  flex: 1;
  min-height: 0;
}

.pane-metrics {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
  margin-bottom: 12px;
}

.pane-metric {
  background: rgba(255,255,255,0.035);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 9px;
}

.pane-metric strong {
  display: block;
  font-size: 16px;
  line-height: 1.1;
  color: var(--text-primary);
}

.pane-metric span {
  display: block;
  margin-top: 4px;
  color: var(--text-muted);
  font-size: 9px;
  font-weight: 800;
  letter-spacing: .9px;
  text-transform: uppercase;
}

.pane-section-title {
  margin: 14px 0 8px;
  color: var(--text-secondary);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1px;
  text-transform: uppercase;
}

.class-bar-row {
  display: grid;
  grid-template-columns: 90px 1fr 58px;
  gap: 8px;
  align-items: center;
  margin: 7px 0;
  color: var(--text-secondary);
  font-size: 11px;
}

.class-bar-track {
  height: 8px;
  border-radius: 999px;
  background: rgba(255,255,255,0.07);
  overflow: hidden;
}

.class-bar-fill {
  height: 100%;
  border-radius: 999px;
  background: var(--accent-cyan);
}

.pane-action {
  width: 100%;
  justify-content: center;
  margin-top: 12px;
}

@media (max-width: 1180px) {
  .map-workspace { grid-template-columns: 1fr; }
  .map-side-pane { height: auto; min-height: 0; }
}

/* COMPACT MAP-FIRST APP CHROME */
.container {
  padding: 0 8px 8px;
}

header {
  position: sticky;
  top: 0;
  z-index: 35;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  min-height: 44px;
  margin: 0 -8px 6px;
  padding: 7px 10px;
  text-align: left;
  background: rgba(6, 9, 19, 0.88);
  border-bottom: 1px solid var(--border);
  backdrop-filter: blur(18px);
}

header::after,
header p,
.top-network-banner {
  display: none;
}

.header-badge {
  flex: 0 1 auto;
  margin: 0;
  padding: 4px 8px;
  max-width: 48vw;
  border-radius: 999px;
  font-size: 9px;
  letter-spacing: .8px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.header-badge svg {
  width: 12px;
  height: 12px;
  flex: 0 0 auto;
}

header h1 {
  flex: 0 0 auto;
  margin: 0;
  font-size: 1rem;
  line-height: 1.1;
  letter-spacing: 0;
  white-space: nowrap;
}

.kpi-row {
  grid-template-columns: repeat(8, minmax(108px, 1fr));
  gap: 6px;
  margin: 6px 0;
  padding-bottom: 2px;
  overflow-x: auto;
}

.kpi-card {
  min-width: 108px;
  padding: 7px 8px;
  text-align: left;
  border-radius: 8px;
}

.kpi-card::before {
  height: 2px;
}

.kpi-card:hover {
  transform: translateY(-1px);
}

.kpi-value {
  margin-bottom: 2px;
  font-size: 1rem;
  letter-spacing: 0;
}

.kpi-label {
  font-size: 8px;
  letter-spacing: .65px;
}

.toolbar {
  position: sticky;
  top: 44px;
  z-index: 30;
  gap: 6px;
  margin: 6px 0;
  padding: 6px;
  border-radius: 8px;
}

.toolbar label {
  display: none;
}

.toolbar select,
.toolbar input[type="text"] {
  min-width: 132px;
  height: 30px;
  padding: 5px 8px;
  border-radius: 7px;
  font-size: 11px;
}

.search-wrapper {
  flex: 1 1 220px;
  position: relative;
}
.search-wrapper input[type="text"] {
  width: 100%;
}
.search-suggestions {
  position: absolute;
  top: calc(100% + 4px);
  left: 0;
  right: 0;
  background: var(--gradient-card);
  backdrop-filter: blur(24px);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  max-height: min(360px, 58vh);
  overflow-y: auto;
  z-index: 10000;
  box-shadow: 0 8px 32px rgba(0,0,0,0.5);
  display: flex;
  flex-direction: column;
}
.search-suggestion-item {
  padding: 8px 12px;
  cursor: pointer;
  border-bottom: 1px solid rgba(255,255,255,0.05);
}
.search-suggestion-item:last-child {
  border-bottom: none;
}
.search-suggestion-item:hover, .search-suggestion-item.active {
  background: rgba(84,116,255,0.15);
}
.search-suggestion-item mark {
  background: rgba(95, 232, 255, 0.18);
  color: var(--cyan);
  border-radius: 3px;
  padding: 0 2px;
}
.search-suggestion-title {
  color: var(--text-primary);
  font-weight: 600;
  font-size: 13px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.search-suggestion-type {
  color: var(--cyan);
  font-size: 9px;
  text-transform: uppercase;
  letter-spacing: .75px;
  margin-left: 6px;
}
.search-suggestion-sub {
  color: var(--text-muted);
  font-size: 11px;
  margin-top: 2px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.btn-group {
  gap: 5px;
  flex-wrap: wrap;
}

.btn {
  min-height: 30px;
  padding: 6px 8px;
  border-radius: 7px;
  font-size: 10px;
  letter-spacing: .65px;
}

.btn svg {
  width: 13px;
  height: 13px;
}

.tabs {
  gap: 4px;
  margin: 6px 0;
  padding: 4px;
  border-radius: 8px;
}

.tab {
  flex: 0 0 auto;
  padding: 7px 9px;
  border-radius: 7px;
  font-size: 9px;
  letter-spacing: .6px;
}

#panel-spatial-map > .card {
  padding: 8px !important;
}

#panel-spatial-map > .card > .card-title,
#panel-spatial-map > .card > .card-subtitle {
  display: none;
}

.map-stat-row {
  grid-template-columns: repeat(auto-fit, minmax(118px, 1fr));
  gap: 6px;
  margin-bottom: 6px;
}

.map-stat {
  padding: 6px 8px;
  border-radius: 7px;
}

.map-stat strong {
  font-size: 13px;
}

.map-stat span {
  margin-top: 2px;
  font-size: 7.5px;
  letter-spacing: .65px;
}

.map-bridge-filter-strip {
  display: grid;
  grid-template-columns: repeat(7, minmax(100px, 1fr));
  gap: 8px;
  margin: 0 0 6px;
  padding: 6px;
  background: rgba(8, 13, 29, 0.72);
  border: 1px solid var(--border);
  border-radius: 8px;
}

.map-bridge-filter-strip select {
  width: 100%;
  min-width: 0;
  height: 30px;
  padding: 5px 9px;
  border: 1px solid rgba(84,116,255,0.35);
  border-radius: 7px;
  background: rgba(255,255,255,0.035);
  color: var(--text-primary);
  font-family: inherit;
  font-size: 11px;
  font-weight: 700;
  outline: none;
}

.map-bridge-filter-strip select:focus {
  border-color: var(--accent-cyan);
  box-shadow: 0 0 0 2px rgba(0,229,255,0.18);
}

.map-bridge-filter-strip select option {
  background: #111111;
  color: #ffffff;
}

.pane-chart-card {
  margin-top: 10px;
  padding: 10px;
  background: rgba(255,255,255,0.03);
  border: 1px solid var(--border);
  border-radius: 8px;
}

.pane-chart-title {
  margin-bottom: 8px;
  color: var(--text-secondary);
  font-size: 9px;
  font-weight: 800;
  letter-spacing: .8px;
  text-transform: uppercase;
}

.pane-chart-card canvas {
  width: 100%;
  height: 220px;
}

.pane-mini-table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 8px;
  font-size: 10px;
}

.pane-mini-table th,
.pane-mini-table td {
  padding: 6px;
  border-bottom: 1px solid rgba(255,255,255,0.06);
  text-align: left;
}

.pane-mini-table th {
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: .7px;
}

.map-workspace {
  --map-pane-width: 430px;
  grid-template-columns: minmax(0, 1fr) 8px minmax(320px, var(--map-pane-width));
  gap: 6px;
}

.map-stage {
  height: calc(100vh - 178px);
  min-height: 610px;
}

.map-pane-resizer {
  width: 8px;
  min-height: 610px;
  border-radius: 999px;
  background: linear-gradient(180deg, transparent 8%, rgba(84,116,255,0.22), transparent 92%);
  cursor: col-resize;
  position: relative;
}

.map-pane-resizer::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 3px;
  height: 54px;
  transform: translate(-50%, -50%);
  border-radius: 999px;
  background: rgba(0,229,255,0.55);
  box-shadow: 0 0 12px rgba(0,229,255,0.35);
}

.map-pane-resizer:hover::after,
.map-pane-resizer.is-dragging::after {
  background: var(--accent-cyan);
}

.map-side-pane {
  height: calc(100vh - 178px);
  min-height: 610px;
  min-width: 320px;
}

@media (max-width: 1180px) {
  .map-stage {
    height: calc(100vh - 190px);
    min-height: 520px;
  }

  .map-side-pane {
    height: auto;
    min-height: 0;
  }

  .map-pane-resizer {
    display: none;
  }

  .map-workspace {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 760px) {
  header {
    gap: 8px;
  }

  .header-badge {
    max-width: 34vw;
  }

  header h1 {
    font-size: .9rem;
  }

  .toolbar {
    top: 42px;
  }

  .map-bridge-filter-strip {
    grid-template-columns: 1fr;
  }
}

.priority-high { color: var(--accent-rose); font-weight: 800; }
.priority-medium { color: var(--accent-amber); font-weight: 800; }
.priority-watch { color: var(--accent-cyan); font-weight: 800; }
.priority-low { color: var(--accent-emerald); font-weight: 800; }
  /* USER REQUEST: "bridge inventory and bridge traffic analytics should just be tables of all available data" */
  #panel-bridge-table .chart-row,
  #panel-bridge-table .traffic-context-grid,
  #panel-bridge-traffic .chart-row,
  #panel-bridge-traffic .traffic-context-grid,
  #panel-bridge-traffic > .card:not(:has(table)) {
    display: none !important;
  }
  
  /* Hide any chart containers that might be inside a card that is kept for its table */
  #panel-bridge-table .chart-container,
  #panel-bridge-traffic .chart-container {
    display: none !important;
  }

/* Origin-Destination Matrix ΓÇö Per-Bridge Crossing */
.od-panel-layout {
  display: flex;
  flex-direction: column;
  gap: 16px;
}
.od-summary-row {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 10px;
}
.od-stat-card {
  background: rgba(255,255,255,0.03);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 14px 16px;
  display: flex;
  flex-direction: column;
}
.od-stat-card strong {
  font-size: 22px;
  font-weight: 800;
  color: var(--accent-cyan);
  font-variant-numeric: tabular-nums;
}
.od-stat-card span {
  font-size: 9px;
  text-transform: uppercase;
  letter-spacing: .8px;
  color: var(--text-muted);
  margin-top: 4px;
}
.od-controls-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  align-items: center;
  padding: 10px 14px;
  background: rgba(8,13,29,0.72);
  border: 1px solid var(--border);
  border-radius: 10px;
}
.od-controls-bar select,
.od-controls-bar input {
  height: 32px;
  padding: 4px 10px;
  border: 1px solid rgba(84,116,255,0.35);
  border-radius: 7px;
  background: rgba(255,255,255,0.035);
  color: var(--text-primary);
  font-family: inherit;
  font-size: 12px;
  font-weight: 600;
  outline: none;
  min-width: 140px;
}
.od-controls-bar select:focus,
.od-controls-bar input:focus {
  border-color: var(--accent-cyan);
  box-shadow: 0 0 0 2px rgba(0,229,255,0.18);
}
.od-controls-bar select option { background: #111; color: #fff; }
.od-controls-bar .od-search-input { flex: 1 1 200px; min-width: 160px; }
.od-view-toggle {
  display: flex;
  gap: 0;
  border-radius: 7px;
  overflow: hidden;
  border: 1px solid rgba(84,116,255,0.35);
}
.od-view-toggle button {
  padding: 6px 14px;
  background: transparent;
  color: var(--text-muted);
  border: none;
  font-family: inherit;
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: .6px;
  cursor: pointer;
  transition: all .2s;
}
.od-view-toggle button.active {
  background: rgba(84,116,255,0.25);
  color: var(--accent-cyan);
}
.od-view-toggle button:hover:not(.active) {
  background: rgba(255,255,255,0.04);
  color: var(--text-primary);
}
.od-table-wrap {
  overflow: auto;
  max-height: 65vh;
  border-radius: 10px;
  border: 1px solid var(--border);
  background: rgba(255,255,255,0.015);
}
.od-table {
  width: 100%;
  border-collapse: collapse;
  white-space: nowrap;
  font-size: 12px;
}
.od-table th {
  position: sticky;
  top: 0;
  background: rgba(10,14,28,0.97);
  backdrop-filter: blur(8px);
  z-index: 2;
  padding: 10px 14px;
  text-align: left;
  font-size: 10px;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: .7px;
  color: var(--text-muted);
  border-bottom: 2px solid var(--border);
  cursor: pointer;
  user-select: none;
}
.od-table th:hover { color: var(--accent-cyan); }
.od-table th .sort-arrow { opacity: 0.3; margin-left: 4px; font-size: 9px; }
.od-table th.sorted .sort-arrow { opacity: 1; color: var(--accent-cyan); }
.od-table td {
  padding: 8px 14px;
  border-bottom: 1px solid rgba(255,255,255,0.04);
  color: var(--text-primary);
  font-variant-numeric: tabular-nums;
}
.od-table tbody tr:hover { background: rgba(84,116,255,0.08); }
.od-table .od-col-adt { text-align: right; font-weight: 700; color: var(--accent-cyan); }
.od-table .od-col-origin, .od-table .od-col-dest { font-weight: 600; }
.od-table .od-col-bridge { color: var(--text-muted); font-size: 11px; }
.od-table .od-col-year { text-align: center; }
.od-heatmap-wrap {
  overflow: auto;
  max-height: 65vh;
  border-radius: 10px;
  border: 1px solid var(--border);
  background: rgba(255,255,255,0.015);
}
.od-heatmap { border-collapse: collapse; font-size: 10px; }
.od-heatmap th {
  position: sticky;
  background: rgba(10,14,28,0.97);
  z-index: 1;
  padding: 6px 8px;
  font-size: 9px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: .5px;
  color: var(--text-muted);
  border-bottom: 2px solid var(--border);
  border-right: 1px solid rgba(255,255,255,0.04);
  text-align: center;
  max-width: 80px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.od-heatmap thead th { top: 0; }
.od-heatmap th:first-child {
  position: sticky; left: 0; z-index: 3; text-align: left;
  border-right: 2px solid var(--border); min-width: 100px;
}
.od-heatmap thead th:first-child { z-index: 4; }
.od-heatmap td {
  padding: 5px 6px; text-align: center;
  border-bottom: 1px solid rgba(255,255,255,0.03);
  border-right: 1px solid rgba(255,255,255,0.03);
  font-variant-numeric: tabular-nums; min-width: 52px;
}
.od-heatmap tbody td:first-child {
  position: sticky; left: 0; background: rgba(10,14,28,0.97); z-index: 2;
  font-weight: 700; text-align: left; border-right: 2px solid var(--border);
  color: var(--text-primary); font-size: 10px;
}
.od-heat-0 { color: rgba(255,255,255,0.15); }
.od-heat-1 { background: rgba(56,189,248,0.08); color: rgba(255,255,255,0.6); }
.od-heat-2 { background: rgba(56,189,248,0.16); color: rgba(255,255,255,0.8); }
.od-heat-3 { background: rgba(99,102,241,0.25); color: #fff; }
.od-heat-4 { background: rgba(99,102,241,0.4); color: #fff; font-weight: 600; }
.od-heat-5 { background: rgba(99,102,241,0.6); color: #fff; font-weight: 700; }
.od-heat-6 { background: rgba(139,92,246,0.75); color: #fff; font-weight: 800; }
.od-heat-7 { background: rgba(168,85,247,0.85); color: #fff; font-weight: 800; text-shadow: 0 1px 4px rgba(0,0,0,0.4); }
.od-pagination {
  display: flex; align-items: center; justify-content: space-between;
  padding: 10px 14px; font-size: 11px; color: var(--text-muted);
}
.od-pagination button {
  padding: 5px 14px; background: rgba(84,116,255,0.15);
  border: 1px solid rgba(84,116,255,0.3); border-radius: 6px;
  color: var(--text-primary); font-family: inherit; font-size: 11px;
  font-weight: 700; cursor: pointer; transition: all .15s;
}
.od-pagination button:disabled { opacity: 0.3; cursor: default; }
.od-pagination button:not(:disabled):hover { background: rgba(84,116,255,0.3); }
.od-empty-state {
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  padding: 60px 20px; color: var(--text-muted); text-align: center; gap: 12px;
}
.od-empty-state svg { width: 48px; height: 48px; opacity: 0.3; }
.od-empty-state p { font-size: 13px; max-width: 400px; }
.od-bridge-link {
  color: var(--accent-cyan);
  text-decoration: underline;
  cursor: pointer;
  margin-right: 8px;
  font-weight: 700;
  transition: color 0.15s ease, text-shadow 0.15s ease;
}
.od-bridge-link:hover {
  color: #fff !important;
  text-shadow: 0 0 6px var(--accent-cyan);
}
</style>
  <script src="bridge_data.js" defer></script>
  <script src="water_data.js" defer></script>
  <script src="bridge_works_data.js" defer></script>
  <script src="chart.umd.js" defer></script>
  <script src="app.js" defer></script>
</head>
<body>

<div class="container">
  <header>
    <div class="header-badge">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 21h18M3 7l9-4 9 4M4 7v10h16V7"/><rect x="8" y="11" width="3" height="6"/><rect x="13" y="11" width="3" height="6"/></svg>
      Ministry of Works and Transport - Department of National Roads
    </div>
    <h1>Bridge Traffic &amp; Network Analytics</h1>
    <p>Focused bridge crossing dashboard showing host road links, nearby traffic count stations, classified ADT, and wider-network loading context.</p>
  </header>





  <!-- Toolbar -->
  <div class="toolbar">
    <label for="bridgeSearch">Search</label>
    <div class="search-wrapper">
      <input type="text" id="bridgeSearch" placeholder="Search bridge name or number..." autocomplete="off" spellcheck="false" role="combobox" aria-autocomplete="list" aria-expanded="false" aria-controls="searchSuggestions">
      <div id="searchSuggestions" class="search-suggestions" style="display:none;"></div>
    </div>
    <div class="btn-group">
      <button class="btn" id="btnDownloadCSV" title="Download active tab data as CSV">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 3v4a1 1 0 001 1h4"/><path d="M17 21H7a2 2 0 01-2-2V5a2 2 0 012-2h7l5 5v11a2 2 0 01-2 2z"/><path d="M12 11v6"/><path d="M9 14l3 3 3-3"/></svg>
        Export CSV
      </button>
      <button class="btn btn-primary" id="btnDownloadPNG" title="Download active chart as PNG image">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="M21 15l-5-5L5 21"/></svg>
        Save PNG
      </button>
      <button class="btn" id="btnDownloadKML" title="Download corrected bridge database as KML">KML</button>
      <button class="btn" id="btnDownloadSHP" title="Download corrected bridge database as zipped shapefile">SHP</button>
        <button class="btn" id="btnDownloadPDF" title="Download bridge traffic, vehicle-class, and growth PDF">PDF</button>
      <button class="btn" id="btnDownloadPeriodCSV" title="Download vehicle-class period summary as CSV">Period CSV</button>
      <button class="btn" id="btnDownloadXLSX" title="Download formula workbook companion">XLSX</button>
    </div>
  </div>

  <!-- Tabs -->
  <div class="tabs" role="tablist">
    <button class="tab active" data-tab="spatial-map" role="tab">Geospatial</button>
    <button class="tab" data-tab="bridge-table" role="tab">Bridge Inventory</button>
    <button class="tab" data-tab="bridge-traffic" role="tab">Bridge Traffic Analytics</button>
      <button class="tab" data-tab="od-matrix" role="tab">Origin-Destination Matrix</button>
    <div style="margin-left: auto; display: flex; align-items: center; padding-right: 4px;">
      <button class="btn btn-outline" id="btnClearFiltersSort" title="Clear all filters and reset sorting" style="padding: 4px 10px; font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; border-color: rgba(255, 60, 60, 0.4); color: #ff6b6b; height: 26px; line-height: 1;">Clear Filters &amp; Sort</button>
    </div>
  </div>

  <!-- Global Filters (apply across all tabs) -->
  <div class="map-bridge-filter-strip" aria-label="Bridge traffic analytics filters">
    <select id="regionFilter"><option value="all">All Regions</option></select>
    <select id="roadClassFilter">
      <option value="all">All Classes</option>
      <option value="M">M - Motorway</option>
      <option value="A">A - National</option>
      <option value="B">B - Regional</option>
      <option value="C">C - Collector</option>
    </select>
    <select id="bridgeTrafficNameSelect" title="Bridge name"></select>
    <select id="bridgeTrafficNoSelect" title="Bridge number"></select>
    <select id="bridgeTrafficRegionSelect" title="Region"></select>
    <select id="bridgeTrafficStationSelect" title="Station"></select>
    <select id="bridgeTrafficLinkSelect" title="Road link name"></select>
  </div>

  <!-- SPATIAL NETWORK MAP PANEL -->
  <div class="panel active" id="panel-spatial-map">
      <div class="card" style="padding: 14px;">
      <div class="card-title">Interactive Spatial Network Map</div>
      <div class="card-subtitle">Single map with Esri World Imagery + reference labels when online, and an embedded offline vector fallback when tiles are unavailable. The 2026 national road network, bridge host roads, bridges, water layers, labels, and traffic count stations are drawn from embedded local data.</div>


      <div class="map-workspace">
      <div class="map-stage">
        <!-- Canvas Element -->
        <canvas id="mapCanvas" style="width: 100%; height: 100%; cursor: grab; display: block;"></canvas>

        <!-- Map Controls -->
        <div style="position: absolute; bottom: 12px; right: 12px; display: flex; flex-direction: column; gap: 6px; z-index: 10;">
          <button class="btn" id="btnMapZoomIn" style="padding: 6px 10px; min-width: 36px; font-weight: 800; font-size: 16px;">+</button>
          <button class="btn" id="btnMapZoomOut" style="padding: 6px 10px; min-width: 36px; font-weight: 800; font-size: 16px;">-</button>
          <button class="btn" id="btnMapClearSelection" style="padding: 6px 10px; font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px;">Clear</button>
          <button class="btn" id="btnMapReset" style="padding: 6px 10px; font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px;">Reset</button>
        </div>

        <!-- Legend Overlay -->
        <div style="position: absolute; top: 12px; left: 12px; background: rgba(18, 22, 35, 0.88); backdrop-filter: blur(12px); border: 1px solid var(--border); border-radius: 12px; padding: 14px 16px; pointer-events: none; z-index: 998; display: flex; flex-direction: column; gap: 8px; box-shadow: 0 8px 32px rgba(0,0,0,0.45); min-width: 170px;">
          <div style="font-size: 11px; font-weight: 800; text-transform: uppercase; color: #26e6f6; letter-spacing: 0.8px; padding-bottom: 6px; border-bottom: 1px solid rgba(255,255,255,0.08); margin-bottom: 4px;">ROAD CLASS LEGEND</div>

          <div style="display: flex; align-items: center; gap: 10px; font-size: 11px; font-weight: 700; color: #ffffff;">
            <span style="display: inline-block; width: 16px; height: 5px; background: #ef4444; border-radius: 999px;"></span>
            <span>Class A - International Trunk Roads</span>
          </div>
          <div style="display: flex; align-items: center; gap: 10px; font-size: 11px; font-weight: 700; color: #ffffff;">
            <span style="display: inline-block; width: 16px; height: 5px; background: #f97316; border-radius: 999px;"></span>
            <span>Class B - National Trunk Roads</span>
          </div>
          <div style="display: flex; align-items: center; gap: 10px; font-size: 11px; font-weight: 700; color: #ffffff;">
            <span style="display: inline-block; width: 16px; height: 5px; background: #22c55e; border-radius: 999px;"></span>
            <span>Class C - Primary Roads</span>
          </div>
          <div style="display: flex; align-items: center; gap: 10px; font-size: 11px; font-weight: 700; color: #ffffff;">
            <span style="display: inline-block; width: 16px; height: 5px; background: #a855f7; border-radius: 999px;"></span>
            <span>Class M - Expressways</span>
          </div>

          <div style="margin-top: 2px; padding-top: 6px; border-top: 1px solid rgba(255,255,255,0.08); display: flex; flex-direction: column; gap: 5px; font-size: 10px; color: rgba(255,255,255,0.6);">
            <div style="display: flex; align-items: center; gap: 8px;">
              <span style="position: relative; display: inline-block; width: 12px; height: 12px; transform: rotate(45deg); border: 2px solid #fff; background: #00e5ff; box-shadow: 0 0 0 2px rgba(2,6,23,0.75), 0 0 10px rgba(0,229,255,0.65);">
                <span style="position:absolute; left:1px; right:1px; top:4px; height:2px; background:#06101f;"></span>
              </span>
              <span>Bridge crossings</span>
            </div>
            <div style="display: flex; align-items: center; gap: 8px;">
              <span style="display: inline-block; width: 8px; height: 8px; border: 1.5px solid #fff; border-radius: 50%; background: #f43f5e;"></span>
              <span>ATC stations</span>
            </div>
            <div style="display: flex; align-items: center; gap: 8px;">
              <span style="display: inline-block; width: 8px; height: 8px; border: 1.5px solid #fff; border-radius: 50%; background: #f59e0b;"></span>
              <span>Legacy ATC stations</span>
            </div>
            <div style="display: flex; align-items: center; gap: 8px;">
              <span style="display: inline-block; width: 6px; height: 6px; border: 1px solid #f43f5e; border-radius: 50%; background: #fff;"></span>
              <span>Manual count stations</span>
            </div>
            <div style="display: flex; align-items: center; gap: 8px;">
              <span style="display: inline-block; width: 16px; height: 2px; background: linear-gradient(90deg, rgba(0,229,255,0.05), rgba(0,229,255,0.85)); border-radius: 999px;"></span>
              <span>Animated traffic on road links</span>
            </div>
            <div style="display: flex; align-items: center; gap: 8px;">
              <span style="display: inline-block; width: 16px; height: 5px; background: rgba(56,189,248,0.45); border-radius: 999px;"></span>
              <span>Lakes and major rivers</span>
            </div>
            <div id="mapTileStatus" style="font-weight: 700; color: var(--accent-cyan); font-size: 9px; margin-top: 1px;">Map ready - imagery loads in background</div>
          </div>
        </div>

        <!-- Float Tooltip -->
        <div id="mapTooltip" style="position: absolute; pointer-events: none; background: rgba(6, 9, 19, 0.95); border: 1px solid var(--border-glow); border-radius: 12px; padding: 12px 16px; color: var(--text-primary); font-family: inherit; font-size: 11px; z-index: 20; display: none; min-width: 200px; box-shadow: 0 10px 30px rgba(0,0,0,0.5);">
          <div id="mTooltipTitle" style="font-weight: 800; color: #fff; margin-bottom: 4px; font-size: 12px;">-</div>
          <div style="display:flex; justify-content:space-between; margin-bottom: 2px;"><span style="color:var(--text-muted)">Region:</span> <span id="mTooltipRegion" style="font-weight:600">-</span></div>
          <div style="display:flex; justify-content:space-between; margin-bottom: 2px;"><span style="color:var(--text-muted)">River:</span> <span id="mTooltipRiver" style="font-weight:600">-</span></div>
          <div style="display:flex; justify-content:space-between; margin-bottom: 2px;"><span style="color:var(--text-muted)">Road Class:</span> <span id="mTooltipClass" style="font-weight:600">-</span></div>
          <div style="display:flex; justify-content:space-between;"><span style="color:var(--text-muted)">AADT:</span> <span id="mTooltipAadt" style="color:var(--accent-cyan); font-weight:700">-</span></div>
        </div>

      </div>
      <div class="map-pane-resizer" id="mapPaneResizeHandle" title="Drag to resize analytics pane" aria-label="Resize analytics pane"></div>
      <aside class="map-side-pane" id="bridgeAnalyticsPane">
        <div class="pane-header">
          <div class="pane-eyebrow">Selected Bridge Traffic Analytics</div>
          <div class="pane-title" id="paneBridgeName">Select a bridge</div>
          <div class="pane-subtitle" id="paneBridgeSubtitle">Click any bridge point on the map to inspect projected ADT, vehicle class loading, and bridge condition context.</div>
        </div>
        <div class="pane-body" id="paneBridgeBody">
          <div class="empty-state" style="padding:28px 8px">
            <p>No bridge selected.</p>
          </div>
        </div>
      </aside>
      </div>
    </div>
  </div>
  </div>
